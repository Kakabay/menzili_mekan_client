import React from 'react';

const Button = () => {
  return (
    <button className="uppercase text-[18px] tracking-[1%] leading-[24.3px] bg-blue text-center px-[32px] py-[16px] rounded-full">
      showreel
    </button>
  );
};

export default Button;
